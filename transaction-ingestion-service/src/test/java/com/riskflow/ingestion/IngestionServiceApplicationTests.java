package com.riskflow.ingestion;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// Basic smoke test — verifies the Spring application context loads
// without errors. If any bean configuration, dependency injection,
// or property binding is broken, this test will catch it.
@SpringBootTest
class IngestionServiceApplicationTests {

    @Test
    void contextLoads() {
    }
}